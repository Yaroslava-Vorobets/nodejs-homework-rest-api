const {validateBody} = require('./validateBody')
const isValidId = require('./isValidId')
const {validaFavoriteteBody} = require('./validateBody')

module.exports = {
    validateBody,
    isValidId,
    validaFavoriteteBody,
}